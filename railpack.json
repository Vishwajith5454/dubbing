{
  "$schema": "https://schema.railpack.com",     
  "setup": [
    // (only if you need extra system packages–usually not required)
  ],
  "install": [
    "pip install --upgrade pip",                  // ensure latest pip
    "pip install -r requirements.txt"             // install your deps
  ],
  "start": [
    "gunicorn app:app --bind 0.0.0.0:$PORT"       // start your Flask app
  ]
}

